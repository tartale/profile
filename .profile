#!/usr/bin/env bash
# shellcheck disable=SC1090,SC1091

source "${PROFILE}/.prerequisites"
source "${PROFILE}/.shell-completion.${SHELLNAME}"
source "${PROFILE}/.env"
source "${PROFILE}/.alias"
source "${PROFILE}/.brew"
source "${PROFILE}/.docker-completion"
source "${PROFILE}/.func"
source "${PROFILE}/.gitx"
source "${PROFILE}/.msplit"
source "${PROFILE}/.shims"
source "${PROFILE}/.gox"
source "${PROFILE}/.ssl"
